#!groovy
pipeline{
    agent { node { label "build"} }
    
    steges
      stege{"build"}{
        steps{
          script{
              mvnhome = tool "mvn"
              sh "${${mvnhome} -v}"
          }
        }
      }
}
