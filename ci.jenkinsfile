#!groovy

String mvnoptions = "${env.mvnoptions}"

pipeline{
    agent { node { label "build"} }
    
    stages{
      stage("build"){
        steps{
          script{
              mvnhome = tool "mvn"
              sh "${mvnhome} ${mvnoptions}"
          }
        }
      }
    }
}
